{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}

{% block piedpage %}Fichier index.html.twig, dans le footer {% endblock %}

{% block neuf %}Ceci ne s'affichera pas{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>🤘Je te dis {{ controller_name }}! 🤘</h1>
    {# les doubles accolades {{}} : équivalent à <?= ?> en php #}
    {# les variables en twig, ne st pas introduite par un caractère #}
    <p>{{ texte }}</p>  {# on peut pas écrire ca comme ca, il faut la définir(ici ds testController.php) sinon erreur #}

    {# Les TEST twig : syntaxe : variable is test
    parmi les tests il y a defined, null, empty 
    syntaxe pour le if : 
    {% if condition%}
    {%elseif condition%}/{%else condition%}
    {% endif%}#}
    {% if calcul is defined %}
        <p>Le résultat du calcul est : {{ calcul }}</p>
    {% endif %}
    

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/dorah/myscoreboard/src/Controller/TestController.php'|file_link(0) }}">src/Controller/TestController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/dorah/myscoreboard/templates/test/index.html.twig'|file_link(0) }}">templates/test/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} 
{# au-dessus c'est la fin du {%block body%} #}
